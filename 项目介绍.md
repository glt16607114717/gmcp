# NND-MCP 数据库查询工具

## 功能说明

为 AI 助手提供统一的mcp接口，当前只写了一个mysql工具，后面会持续添加新的功能
本工具完全自研，体验不好随时反馈随时改，市面上有其他好用的mcp也可以考虑集成进来，或者只要第三方提供了API接口，也可以原创开发

## 快速配置

在 Trae IDE 中添加 MCP 服务器：

```json
{
  "mcpServers": {
    "nnd-mcp": {
      "url": "http://mcp.nndrobot.com/mcp",
      "headers": {
        "X-MCP-Username": "用户名",
        "X-MCP-Password": "密码"
      }
    }
  }
}
```

## 账号获取

联系管理员在正式环境 `r_user_auth` 表中创建账号记录，提供用户名和对应的密码哈希值。

## 使用示例

### 基础查询
```json
{
  "environment": "prod",
  "sql": "SELECT * FROM r_user WHERE id = 1"
}
```

### 统计查询
```json
{
  "environment": "test", 
  "sql": "SELECT company_id, COUNT(*) as count FROM r_user GROUP BY company_id"
}
```

## 环境说明

- **dev**：开发环境，支持读写操作
- **test**：测试环境，只读
- **gray**：灰度环境，只读  
- **prod**：正式环境，只读

## 安全机制

- **用户鉴权**：基于正式环境用户表验证
- **权限控制**：开发环境可增删改，其他环境只读
- **危险操作拦截**：自动阻止 DELETE、DROP、ALTER 等危险 SQL

## 持续维护

本工具会持续优化功能，增加更多数据库类型和查询能力。